spring.application.name=securelib

# In-memory H2 database
spring.datasource.url=jdbc:h2:mem:securelib;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa

# VULNERABLE: hardcoded database password committed to source control.
# Remediation: read from an environment variable, e.g. ${DB_PASSWORD}.
spring.datasource.password=Sup3rS3cretDbP@ss2024

spring.sql.init.mode=always
